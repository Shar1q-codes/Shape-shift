/**
 * TypeScript Configuration Root
 * 
 * This is the root TypeScript configuration file that references
 * the app and node-specific configurations. It uses project references
 * to split the configuration into more manageable parts.
 */

{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}