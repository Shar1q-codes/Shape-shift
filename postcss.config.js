/**
 * PostCSS Configuration
 * 
 * PostCSS is a tool for transforming CSS with JavaScript plugins.
 * This configuration sets up plugins for processing CSS files.
 */

export default {
  plugins: {
    // Tailwind CSS plugin - processes Tailwind utility classes
    tailwindcss: {},
    
    // Autoprefixer plugin - adds vendor prefixes for browser compatibility
    autoprefixer: {},
  },
};